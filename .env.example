# Copy to .env (this repo ignores .env*), then fill in values.

# Required to run the MCP server
REFLECT_ACCESS_TOKEN=""

# Optional: pick a default graph id (otherwise the first graph on your account is used)
REFLECT_DEFAULT_GRAPH_ID=""

# Optional: override API base URL
# REFLECT_API_BASE_URL="https://reflect.app/api"

# OAuth helper (optional)
REFLECT_CLIENT_ID=""
REFLECT_CLIENT_SECRET=""
REFLECT_OAUTH_REDIRECT_URI="http://127.0.0.1:8787/callback"

